#include "default.conf"

CONFIG_ZMK_KEYBOARD_NAME="Corne"

CONFIG_ZMK_STUDIO=y

# Override default sleep timeout (60 min instead of 30 min)
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=3600000

# Enable display (uses standard nice_view)
CONFIG_ZMK_DISPLAY=y
