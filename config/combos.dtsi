/*                KEY POSITIONS
 * ╭─────────────────────╮ ╭─────────────────────╮
 * │ LT4 LT3 LT2 LT1 LT0 │ │ RT0 RT1 RT2 RT3 RT4 │
 * │ LM4 LM3 LM2 LM1 LM0 │ │ RM0 RM1 RM2 RM3 RM4 │
 * │ LB4 LB3 LB2 LB1 LB0 │ │ RB0 RB1 RB2 RB3 RB4 │
 * ╰───────────╮ LH1 LH0 │ │ RH0 RH1 ╭───────────╯
 *             ╰─────────╯ ╰─────────╯
 */

// horizontal combos {{{
/* symbols */
ZMK_COMBO(both_par, &both_par, LT0 RT0, BASES, COMBO_TERM_FAST, COMBO_IDLE_FAST)
ZMK_COMBO(both_brk, &both_brk, LM0 RM0, BASES, COMBO_TERM_FAST, COMBO_IDLE_FAST)
ZMK_COMBO(both_brc, &both_brc, LB0 RB0, BASES, COMBO_TERM_FAST, COMBO_IDLE_FAST)

/* left hand */
ZMK_COMBO(esc,      &kp ESC,
    LM3 LM2,        BASES SYM, COMBO_TERM_FAST, COMBO_IDLE_FAST)
ZMK_COMBO(tab,      &kp TAB,
    LM1 LM2,        BASES SYM, COMBO_TERM_FAST, COMBO_IDLE_FAST)


/* right hand */
#ifdef ENABLE_MOUSE_LAYER
#define RIGHT_HAND_LAYERS BASES NUM MOUSE
#else
#define RIGHT_HAND_LAYERS BASES NUM
#endif
ZMK_COMBO(resc,     &kp ESC,
    RM2 RM3,        RIGHT_HAND_LAYERS, COMBO_TERM_FAST, COMBO_IDLE_FAST)
ZMK_COMBO(ret,      &kp RET,
    RM1 RM2,        RIGHT_HAND_LAYERS, COMBO_TERM_FAST, COMBO_IDLE_FAST)
ZMK_COMBO(bspc,     &kp BSPC,
    RT1 RT2,        RIGHT_HAND_LAYERS SYM, COMBO_TERM_FAST, COMBO_IDLE_FAST)
ZMK_COMBO(bspc_w,   &kp RA(BSPC),
    RT0 RT1,        RIGHT_HAND_LAYERS SYM, COMBO_TERM_FAST, COMBO_IDLE_FAST)
/// }}}

// vertical combos {{{
/* left hand */
/* right hand */
/// }}};
